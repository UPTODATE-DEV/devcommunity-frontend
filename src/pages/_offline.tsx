import { Typography } from '@mui/material';
import React from 'react';

const Offline = () => {
  return <Typography variant="h6">Veillez vous connecter à internet</Typography>;
};

export default Offline;
